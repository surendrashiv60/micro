<%= form_for(@sms_template) do |f| %>
  <% if @sms_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sms_template.errors.count, "error") %> prohibited this sms_template from being saved:</h2>

      <ul>
      <% @sms_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table>
	<tr>
		<td><%= f.label :message %></td>
		<td>&nbsp;&nbsp;</td>
		<td><%= f.text_area :message, :rows => 3, :cols => 30 %></td>
	</tr>
 	<tr>
		<td></td>
		<td>&nbsp;&nbsp;</td>
		<td><%= f.submit "Submit", :class => "btn btn-primary" %>&nbsp;&nbsp;<%= link_to 'Back', sms_templates_path, :class => "btn" %></td>
	</tr>
  </table>
<% end %>
